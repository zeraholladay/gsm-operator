# Sample environment configuration for gsm-operator setup scripts.
#
# Copy this file to `.env` and fill in the values, then:
#   export $(grep -v '^#' .env | xargs)

# GKE cluster information
CLUSTER_PROJECT_ID="<cluster project id>"
CLUSTER_NAME="<cluster name>"
CLUSTER_REGION="<cluster region>"

# Project that owns the Workload Identity Pool / OIDC provider: can be the same as CLUSTER_PROJECT_ID
OIDC_PROJECT_ID="<oidc project id>"

# Secrets Project
SECRETS_PROJECT_ID="<project id where sample secrets live>"

# Image registry and tag for gsm-operator
REGISTRY="us-docker.pkg.dev/<gar project>/gsm-operator"
IMAGE_TAG="test"
